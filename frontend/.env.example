# Backend API (optional). Leave empty in dev to use Vite proxy: /api → http://127.0.0.1:8000
# VITE_API_BASE_URL=http://127.0.0.1:8000
#
# Docker(frontend 이미지 빌드): Dockerfile에서 VITE_API_BASE_URL 비움 → 브라우저가 /api 로 호출하고 nginx가 api 컨테이너로 넘김.
